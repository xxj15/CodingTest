for _ in range(10):
  tc = input()
  target = input()
  text = input()

  cnt = 0

  for i in range(len(text) - len(target) + 1):
    if text[i:i+len(target)] == target:
      cnt += 1

  print(f'#{tc} {cnt}')