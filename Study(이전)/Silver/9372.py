# 상근이의 여행
import sys
input = sys.stdin.readline

T = int(input()) # 테스트 케이스 수

for _ in range(T): 
  N, M = map(int, input().split()) # N : 국가의 수, M : 비행기 종류 

  for _ in range(M): 
        input()
  print(N-1)